# Core dependencies - only what's actually imported
numpy>=1.21.0
scipy>=1.7.0
torch>=1.9.0
cryptography>=3.4.0

# Testing and development (optional)
pytest>=6.2.0
pytest-cov>=2.12.0

# Performance monitoring (optional)
memory-profiler>=0.58.0

# Note: Removed problematic dependencies:
# - mine-pytorch (not needed - custom implementation exists)
# - py_ecc (not imported anywhere)
# - scikit-learn (not imported)
# - secrets (built-in Python module)
# - psutil (not imported)
# - black, flake8 (development tools)
# - sphinx (documentation - not needed for runtime)
# - line-profiler (not imported)